<%= simple_form_for [:admin, @class_range] do |f| %>
<%= hidden_field_tag :referer_url, request.env["HTTP_REFERER"] %>
<%= f.input :position %>
<%= f.input :range %>
<%= f.input :size %>
<br>
<div class="actions">
	<%= f.submit "Submit", :class => "btn btn-success" %>
</div>

<% end %>
